project(
        'kronkrt',
        'cpp',
        default_options : ['cpp_std=c++17'],
        version : '0.0.1',
        license : 'GPL'
        )


subdir('src')

meson.add_install_script('install.sh')