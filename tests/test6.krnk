# functions

fn factoriel(n: reel) reel {
    Si(n < 2) alors {
        ret 1;
        afficher(100);
    }
  
    Sinon {
        ret n * factoriel(n-1); 
    }

    afficher(2);
}

afficher(factoriel(5));
